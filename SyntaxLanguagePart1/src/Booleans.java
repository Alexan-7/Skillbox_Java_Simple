public class Booleans {
    public static void main(String[] args) {
        int maxWeight = 5000; // максимально допустимый вес заказа в граммах
        int minCost = 1000; // (минимальная стоимость заказа

        int weight = 3200;
        int cost = 750;

        //   if (weight <= maxWeight && cost >= minCost) {
        //       System.out.println("Доставка бесплатная");

        // if (weight > maxWeight || cost < minCost) {
        //     System.out.println("Доставка платная");

        boolean isDeliveryFree = false;

        if (!isDeliveryFree) {
            System.out.println("Доставка платная");
        }
    }
}
